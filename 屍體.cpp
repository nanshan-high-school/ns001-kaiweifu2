#include <iostream>
#include<stack>
using namespace std;
int main() {
  stack<int>s;
  int totalbook, totalmove, move, book;
  int num[totalbook];
  
  cout<<"請輸入總共要放的書:";
  cin>>totalbook;
  cout<<"請輸入動作次數:";
  cin>>totalmove;
  for(int i = 0 ; i <totalmove ; i++){
    cin>>book>>move;
    if(move == 'a'){
       
    }
    else if(move == 'b'){
      
    }
    
  }
}
